spring:
  application:
    name: exemplo-hystrix-server
  cloud:
    config:
      uri: http://localhost:9090

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, info, health